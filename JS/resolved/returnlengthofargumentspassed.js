/**
 * @return {number}
*/
 
var argumentsLength = function(...args) {
    return args.length;
};

/**
 * argumentsLength(1, 2, 3); // 3
 */

    /*
    let counter = 0;
    for(let i=0; i<args.length; i++){
        counter += 1;
    }
    return counter;
    */

/**
 * argumentsLength(1, 2, 3); // 3
 */