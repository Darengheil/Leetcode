/**
 * @param {string} key
 * @param {string} message
 * @return {string}
 */
var decodeMessage = function(key, message) {
    let test = key.replace(/\s/g, '');
    console.log(test);
    for(let i =0; i < test.length; i++){
    }
};

decodeMessage("the quick brown fox jumps over the lazy dog", "vkbs bs t suepuv");