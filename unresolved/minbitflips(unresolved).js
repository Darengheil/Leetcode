/**
 * @param {number} start
 * @param {number} goal
 * @return {number}
 */
var minBitFlips = function(start, goal) {
    let startgoal = start.toString(2);
    let bitgoal = goal.toString(2);    
    let counter = 0;

    
};

minBitFlips(10,7);